{
  "entities": {
    "BuoySnapshot": {
      "title": "Buoy Snapshot",
      "description": "A record of buoy conditions at a specific point in time.",
      "type": "object",
      "properties": {
        "buoyId": { "type": "string", "description": "The unique identifier for the buoy (e.g., 'Sammamish')" },
        "timestamp": { "type": "string", "format": "date-time", "description": "The time the data was recorded by the sensor" },
        "recordedAt": { "type": "string", "format": "date-time", "description": "The time the snapshot was saved to our database" },
        "tempC": { "type": "number" },
        "tempF": { "type": "number" },
        "airTempC": { "type": "number" },
        "airTempF": { "type": "number" },
        "windSpeed": { "type": "number" },
        "precipitation": { "type": "number" },
        "humidity": { "type": "number" },
        "lat": { "type": "number" },
        "lon": { "type": "number" },
        "serverTime": { "type": "string", "format": "date-time", "description": "Server-side timestamp" }
      },
      "required": ["buoyId", "timestamp", "recordedAt"]
    },
    "ApiKey": {
      "title": "API Key",
      "description": "Developer API keys for external access control to retrieve buoy data.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The Firebase Authentication UID of the owner" },
        "active": { "type": "boolean", "description": "Whether the API key is active" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp" },
        "name": { "type": "string", "description": "A custom friendly name for the key" }
      },
      "required": ["uid", "active", "createdAt"]
    }
  },
  "firestore": {
    "/buoy_snapshots/{snapshotId}": {
      "schema": "BuoySnapshot",
      "description": "Collection of historical buoy data snapshots."
    },
    "/api_keys/{keyId}": {
      "schema": "ApiKey",
      "description": "Collection of developer API keys."
    }
  }
}
